-- This migration was restored as a no-op because the directory existed
-- without a migration.sql file, which causes Prisma P3015 during deploy.
-- Article/blog schema changes already exist in earlier migrations.

SELECT 1;
